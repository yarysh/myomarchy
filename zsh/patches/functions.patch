Subject: [PATCH] functions
---
Index: default/bash/functions
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/default/bash/functions b/default/bash/functions
--- a/default/bash/functions	(revision 1e859d37cb7fef6ac687442dc1fe515d01d1302d)
+++ b/default/bash/functions	(date 1759608755196)
@@ -37,16 +37,6 @@
   fi
 }
 
-# Transcode a video to a good-balance 1080p that's great for sharing online
-transcode-video-1080p() {
-  ffmpeg -i $1 -vf scale=1920:1080 -c:v libx264 -preset fast -crf 23 -c:a copy ${1%.*}-1080p.mp4
-}
-
-# Transcode a video to a good-balance 4K that's great for sharing online
-transcode-video-4K() {
-  ffmpeg -i $1 -c:v libx265 -preset slow -crf 24 -c:a aac -b:a 192k ${1%.*}-optimized.mp4
-}
-
 # Transcode any image to JPG image that's great for shrinking wallpapers
 img2jpg() {
   magick $1 -quality 95 -strip ${1%.*}.jpg
